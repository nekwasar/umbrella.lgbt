{
  "name": "umbrella.lgbt",
  "version": "0.1.0",
  "description": "The everything queer app — pre-launch site",
  "private": true,
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "marked": "^14.0.0"
  }
}
