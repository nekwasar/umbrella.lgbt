# Umbrella.lgbt web — local development environment
# Copy to .env.local and fill in. NEVER commit .env.local.

# The backend API origin (for client + server requests)
NEXT_PUBLIC_API_URL=http://localhost:3000
