import { BoardsManager } from '@/components/admin/BoardsManager';

export const metadata = { title: 'Forum Boards | Admin | Umbrella.lgbt' };

export default function AdminForumPage() {
  return <BoardsManager />;
}
