{
  "name": "umbrella.lgbt",
  "version": "0.1.0",
  "description": "The everything queer app — pre-launch site",
  "private": true,
  "scripts": {
    "css": "tailwindcss -i static/styles/main.css -o static/styles/site.css --minify",
    "build": "npm run css && node build.js",
    "seed:status": "node scripts/seed-status.js",
    "check:links": "node scripts/check-links.js"
  },
  "dependencies": {
    "marked": "^14.0.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "tailwindcss": "^3.4.19"
  }
}
